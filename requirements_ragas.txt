# === RAGAS Evaluation Dependencies ===
# Устанавливать командой: pip install -r requirements_ragas.txt

# Ядро RAGAS
ragas>=0.2.0,<0.3.0

# LangChain (нужен RAGAS для обёрток LLM и эмбеддингов)
langchain>=0.2.0
langchain-openai>=0.1.8
langchain-core>=0.2.0

# HuggingFace Datasets — формат данных для RAGAS
datasets>=2.20.0

# Отчёты и визуализация
pandas>=2.0.0
matplotlib>=3.7.0
plotly>=5.17.0
jinja2>=3.1.0
kaleido>=0.2.1         # для сохранения plotly-графиков в PNG

# Уже есть в проекте, но на всякий случай
python-dotenv>=1.0.0
openai>=1.35.0
chromadb>=0.5.0
